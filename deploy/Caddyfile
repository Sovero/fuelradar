# Reverse proxy (R85). Для HTTPS подставьте домен:
# fuelradar.example.com {
#   reverse_proxy frontend:3000
# }
{$APP_DOMAIN::80} {
	handle /api/* {
		reverse_proxy api:8000
	}
	reverse_proxy frontend:3000
}
