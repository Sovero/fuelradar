"""Пользовательские отчёты (T07, §13 №6-7,12-13): `user_reports` → наблюдения.

`POST /reports` сохраняет отчёт и делегирует запись наблюдений
`confidence.service.StatusService` (не дублирует логику T04); GPS < 300 м
(R40) и идемпотентность (R39.1) — здесь.
"""
